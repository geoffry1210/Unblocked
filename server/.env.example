# Provided automatically by Railway's Postgres plugin in production
DATABASE_URL=postgres://user:password@localhost:5432/unblocked

# Port the REST API + WebSocket server listen on
PORT=3001

# Comma-separated list of allowed origins for CORS (your web service's URL)
WEB_ORIGIN=http://localhost:5173
